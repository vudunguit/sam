package com.sam.ui.base

import androidx.lifecycle.ViewModel

open class BaseViewModel : ViewModel() {
    // Add common ViewModel logic here, like a shared loading state or error handling mechanism.
}
